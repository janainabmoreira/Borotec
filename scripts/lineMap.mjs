// Linhas de produto agora vêm da tabela product_lines (Supabase), buscadas
// em tempo de build por buildRoutes.mjs — não há mais um mapa fixo aqui.
// Este arquivo só guarda as rotas verdadeiramente estáticas.
export const STATIC_ROUTES = ['/', '/boroscopios', '/sobre', '/contato', '/privacidade', '/blog'];
